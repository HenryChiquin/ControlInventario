<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MenuPrincipal">

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="64dp"
        android:layout_marginEnd="156dp"
        android:text="Menu Principal"
        android:textSize="20sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/btnIngresoProducto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="84dp"
        android:layout_marginEnd="3dp"
        android:onClick="IngresarProductos"
        android:text="Ingreso de productos"
        app:layout_constraintEnd_toEndOf="@+id/textView"
        app:layout_constraintTop_toBottomOf="@+id/textView" />

    <Button
        android:id="@+id/btnMantenimientoProducto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="67dp"
        android:onClick="MantenimientoProducto"
        android:text="Mantenimiento de productos"
        app:layout_constraintStart_toStartOf="@+id/btnIngresoProducto"
        app:layout_constraintTop_toBottomOf="@+id/btnIngresoProducto" />

    <Button
        android:id="@+id/btnSalidaProducto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="62dp"
        android:onClick="SalidaProducto"
        android:text="Salida de productos"
        app:layout_constraintStart_toStartOf="@+id/btnMantenimientoProducto"
        app:layout_constraintTop_toBottomOf="@+id/btnMantenimientoProducto" />

</androidx.constraintlayout.widget.ConstraintLayout>